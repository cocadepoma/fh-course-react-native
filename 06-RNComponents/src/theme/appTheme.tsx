import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    globalMArgin: {
        paddingHorizontal: 20,
    },
    title: {
        color: '#565656',
        fontSize: 35,
        fontWeight: 'bold',
    },
});